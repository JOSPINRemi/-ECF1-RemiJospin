class Project {
  constructor(id, title, description, dateStart, dateEnd, status) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.dateStart = dateStart;
    this.dateEnd = dateEnd;
    this.status = status;
  }
}

export default Project;
